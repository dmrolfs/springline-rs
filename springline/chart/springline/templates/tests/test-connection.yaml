apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springline.fullname" . }}-test-connection"
  labels:
{{ include "springline.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "springline.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
